title: Engineering Docs POC
description: Proof of concept — storing ADRs and engineering standards in a GitHub repo, published via GitHub Pages.

markdown: kramdown

defaults:
  - scope:
      path: ""
    values:
      layout: default
